import styles from "./Categories.module.scss"

export default function Categories() {
    return (
        <nav className={styles.Categories}>
            <h2>Żadnych kategorii</h2>
        </nav>
    )
}